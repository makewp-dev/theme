# Contributing

## Local development*

1. Set up WordPress locally to your liking.
2. Run a `composer` command? `init`?
3. Require `makewp/theme`, but somehow make it come from a "repository" with path to your local repo of this codebase?

*Obviously needs work
